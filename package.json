{
  "name": "webgpu-spring-mesh",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "description": "Real-time GPU particle-mesh spring simulation (WebGPU + Three.js TSL) with interactive node dragging and a CPU/WebGL fallback.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "three": "^0.185.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.13.0",
    "@types/three": "^0.185.0",
    "eslint": "^9.13.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.10.0",
    "vite": "^6.0.0"
  }
}
